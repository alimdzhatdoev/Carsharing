# Copy to .env and adjust. Paths are relative to project working directory (project root).

PROJECT_ROOT=

# Artifacts (override paths from config.yaml if set)
ARTIFACTS_DIR=artifacts
MODEL_PATH=artifacts/models/model.pt
PREPROCESSOR_PATH=artifacts/encoders/preprocessor.joblib
TRAINING_CONFIG_PATH=artifacts/models/training_config.json

# Inference
CLASSIFICATION_THRESHOLD=0.5
