"""Практическая ценность и ограничения — для блока «зачем всё это»."""

from __future__ import annotations

import streamlit as st

from app.ui.demo_components import page_title


def render() -> None:
    page_title("Практическая ценность")

    st.subheader("Где может применяться такая модель")
    st.markdown(
        """
- **Операционный мониторинг** — приоритизация поездок для выборочной проверки после завершения аренды.  
- **Антифрод** — дополнительный сигнал рядом с правилами и графом связей (не замена расследованию).  
- **Поддержка пользователей** — раннее выявление поездок с высоким риском жалобы.  
- **Планирование ресурсов** — оценка нагрузки на модераторов в разрезе зон и времени суток (при накоплении статистики).  
        """
    )

    st.subheader("Какие решения можно принимать на её основе")
    st.write(
        "Модель выдаёт **вероятность** и **класс** после порога. На этой основе политика сервиса может: "
        "направить поездку в **очередь модерации**, запросить **дополнительную верификацию**, изменить **лимиты** или "
        "оставить стандартный сценарий. Все жёсткие санкции в реальной жизни должны опираться на **регламент** и **человека**, а не только на скор."
    )

    st.subheader("Ограничения текущей версии")
    st.markdown(
        """
- **Демонстрационные данные** синтетические; закономерности и метрики **не переносятся** на конкретного оператора без валидации на реальных логах.  
- **Нет онлайн-мониторинга** дрейфа признаков и качества в проде.  
- **Нет юридической интерпретации** вывода модели; это **вспомогательный** инструмент.  
- Возможны **смещения** по географии, тарифам и профилю пользователей — на проде нужен аудит fairness и бизнес-метрик.  
        """
    )

    st.subheader("Почему в демо используются синтетические данные")
    st.write(
        "Реальные выгрузки каршеринга содержат персональные данные и коммерческую тайну. Для **учебного и защитного** демо "
        "используется генератор с осмысленными правилами таргета, чтобы сохранить **интерпретируемость** признаков без утечки реальных записей."
    )

    st.subheader("Как масштабировать на реальные данные")
    st.markdown(
        """
1. Согласовать **определение** класса 1 с бизнесом (инцидент, chargeback, оценка ≤ N и т.д.).  
2. Построить **ETL** из DWH/логов в контракт колонок, совместимый с `schema.py` / расширить контракт.  
3. Провести **временное** или **клиентское** разбиение выборок, чтобы не было утечки из будущего.  
4. Настроить **мониторинг** качества, калибровку вероятностей и **версионирование** моделей (MLflow, DVC).  
5. Встроить API в **очереди событий** и панели операторов.  
        """
    )
